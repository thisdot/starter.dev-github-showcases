version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - node -v
            - yarn run build
      artifacts:
        baseDirectory: dist/starter-dev-angular
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: angular-apollo-tailwind
