export enum RepositorySearchType {
  Public = 'public',
  Private = 'private',
  Sources = 'sources',
  Forks = 'forks',
  Archived = 'archived',
  Mirrors = 'mirrors',
  Templates = 'templates',
}

export enum RepositorySearchSort {
  LastUpdated = 'updated',
  Name = 'name',
  Stars = 'stars',
}
