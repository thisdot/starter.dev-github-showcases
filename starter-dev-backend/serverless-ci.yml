service: starter-dev-backend
frameworkVersion: '2 || 3'

plugins:
  - serverless-bundle

provider:
  name: aws
  stage: production
  runtime: nodejs14.x
  lambdaHashingVersion: '20201221'

functions:
  api:
    handler: handler.handler
    events:
      - httpApi: '*'
