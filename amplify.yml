version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - yarn run build
      artifacts:
        baseDirectory: dist/starter-dev-angular
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: angular-apollo-tailwind
